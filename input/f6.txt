Message queues are critical in grid computing and are also useful in many kinds of transactional systems. They are not, however, typical across web applications. Accepts messages and passes them on to servers subscribing to the message queue, the sender does not need to identify a recipient or perform any error handling to deal with communication failures. The recipient does not even need to be active at the time the message is sent.
